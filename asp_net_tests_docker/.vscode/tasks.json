{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/asp_net_tests_docker.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}